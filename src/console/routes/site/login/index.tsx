import * as React from "react";

export default () => (
  <div className="page site-explore">
    <div className="page-title">Login Page</div>
    <div className="page-subtitle">Login Page Sub-title.</div>
  </div>
);
