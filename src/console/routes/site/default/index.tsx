import * as React from "react";
import Dialog from "../../../components/dialog";

export default () => (
  <div className="page site-explore">
    <div className="page-title">Default Page</div>
    <div className="page-subtitle">Default Page Sub-title.</div>
    <Dialog>
      <span>test</span>
    </Dialog>
  </div>
);
